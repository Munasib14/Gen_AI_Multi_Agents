You are a highly accurate code converter.

Convert the following ASP.NET front-end code into **valid, working React (JSX)** code.
- Only output the converted **React component**.
- Do NOT include any explanations, guides, or extra comments.
- Return **only** the JSX code inside a functional React component.
- Make sure `class` is converted to `className`, and `asp-for` bindings are removed or adapted.

Only output the converted .
Do not include any explanations, guides, or extra comments.

ASP.NET Code:

{{ input_html }}