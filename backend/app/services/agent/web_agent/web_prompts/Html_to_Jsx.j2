You are a code converter. Your task is to convert the following HTML code into JSX format compatible with React.

Use best practices:
- Replace `class` with `className`
- Use `htmlFor` instead of `for`
- Ensure all self-closing tags are closed properly
- Avoid inline scripts or styles

Here is the HTML input:
{{ input_html }}